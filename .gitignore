# Local test / backup artifacts
*.bak
*.bak-*
*.testbak
*.tmp
config.json.tmp

# Anything captured from SimAppPro while reverse-engineering.
# Its config.json holds AutoLoginSaveAccount: a base64 blob containing the
# account email and password in the clear. Never commit it.
simapppro-config*.json
WWTHID*.log

# Runtime logs
*.log

# Helper runtime state. Written on every detection, owned by the helper,
# never shipped - a missing file correctly means "nothing detected".
Scripts/wctrl-auto-ab-detent-ratio/lib/state.json

# OS / editor
Thumbs.db
desktop.ini
.vscode/
.idea/
TODO.md
POSTS.md
